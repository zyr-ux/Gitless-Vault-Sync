{
  "name": "gitless-vault-sync",
  "version": "1.0.2",
  "description": "Obsidian plugin to sync a vault with GitHub via REST API.",
  "repository": "https://github.com/zyr-ux/Gitless-Vault-Sync",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@zip.js/zip.js": "^2.8.26",
    "esbuild": "^0.28.0",
    "obsidian": "latest",
    "typescript": "^5.5.4"
  }
}
